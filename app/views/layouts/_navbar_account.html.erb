<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li class='navbar-text'><%= current_user.email %></li>
    <li class="dropdown">
      <a href="javascript:true;" class="dropdown-toggle" data-toggle="dropdown">
        Account
        <b class="caret"></b>
      </a>
      <ul class="dropdown-menu">
        <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete, :class => "btn-btn-navbar" %></li>
        <li><%= link_to "Edit Account", edit_user_registration_path %></li>
      </ul>
    </li>
  <% else %>
    <li><%= link_to "Login", new_user_session_path, :class => 'btn-btn-navbar' %></li>
  <% end %>
</ul>
